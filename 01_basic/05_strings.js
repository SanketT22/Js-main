const name1="sanket" 
const age=21
//console.log(name + age +"value") //it is simple and old way to concatenate 
console.log(`my name is ${name1} and my age is ${age}`) // this way is more efficient and redable
/* ways to define strings are 2
   1. const name="sanket"
   2.const name = new string("sanket")*/

   const name = new String('sssanket')
   console.log(name.toUpperCase());
   console.log(name.charAt(2));
   console.log(name.substring(0,5));
